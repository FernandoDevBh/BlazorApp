@inject ISetListViewModel SetsVM

<StateContainer ViewModel="SetsVM" Name="SetsVM">
    <SetList />
</StateContainer>

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await SetsVM.LoadCategories();
        }
    }
}